# chrome-extension-everything-ics

The Google Chrome extension that generate an ics file by using everthing-ics.

## Build

```shell
pnpm install
pnpm run build
```

And load unpack extension from dist/.
